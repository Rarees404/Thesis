# assets/

Static images used in the README, the paper, and demo materials. Not consumed by the running app.

## Layout

- `visualref_enhanced_workflow.png` — system diagram for the paper.
- `coco_*.png` — example query screenshots (named after the source image filenames).
- `a.png`, `b.png`, `c.png` — generic placeholders used in older docs.
